import 'package:image_picker/image_picker.dart';

Future<String> uploadMultipartWeb(
  String cloudName,
  String uploadPreset,
  XFile imageFile,
) {
  throw UnimplementedError('Web upload is only supported on web platform.');
}
