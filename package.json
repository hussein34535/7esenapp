{
    "name": "hesen-proxy",
    "version": "1.0.0",
    "scripts": {
        "build": "if [ -d \"flutter\" ]; then cd flutter && git pull && cd ..; else git clone https://github.com/flutter/flutter.git -b stable --depth 1; fi && ./flutter/bin/flutter config --enable-web && ./flutter/bin/flutter build web --release"
    },
    "dependencies": {
        "http-proxy-middleware": "^2.0.6"
    }
}