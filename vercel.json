{
    "version": 2,
    "builds": [
        {
            "src": "index.html",
            "use": "@vercel/static-build",
            "config": {
                "outputDirectory": "build/web"
            }
        }
    ],
    "installCommand": "wget https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.22.2-stable.tar.xz && tar -xf flutter_linux_3.22.2-stable.tar.xz && export PATH=\"$PATH:`pwd`/flutter/bin\" && flutter config --enable-web && flutter pub get",
    "buildCommand": "flutter build web --release",
    "outputDirectory": "build/web",
    "routes": [
        {
            "handle": "filesystem"
        },
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ],
    "github": {
        "silent": true
    }
} 