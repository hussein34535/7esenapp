{
  "version": 2,
  "builds": [
    {
      "src": "build.sh",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "chmod +x ./install_flutter.sh && /bin/bash ./install_flutter.sh",
        "buildCommand": "/bin/bash ./build.sh",
        "outputDirectory": "build/web"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
} 